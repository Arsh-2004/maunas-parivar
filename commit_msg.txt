fix & improve: community read-more, non-members layout & admin tab

Community Page (Community.js / Community.css):
- Add expandedBios state, toggleBio helper, BIO_LIMIT=25 constant
- Truncate occupation with single read-more button (collapsed by default)
- Clicking expands both full occupation + bio together
- Single collapse button; separate standalone bio button removed
- Add .read-more-btn CSS (inline orange, no background, hover underline)

Non-Members Page (NonMembers.js / NonMembers.css):
- Redesign card layout to match Home page team-card (Prabhandhan committee style)
- Centered portrait photo (160x200px) with orange border + shadow
- Hover lift on card + photo zoom effect
- Centered name, relation tag, detail text
- Grid minmax(220px, 1fr) matching team-grid
- Optional details section open by default

Admin Dashboard (AdminDashboard.js):
- Add Non-Members tab with stats (total / states / places)
- Live search across name, place, state, district, relationship
- Full table with photo, name, age, place, relation, district, state, date
- View modal showing all available fields
- Delete button with confirmation

Bug Fix:
- Fix NonMemberRecord model: addedBy required:true to default:null
  (was blocking all public submissions with 500 error)
